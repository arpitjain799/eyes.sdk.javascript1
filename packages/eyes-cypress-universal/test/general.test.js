const {UniversalClient} = require('../src/plugin/universal/universal-client');

const universal = new UniversalClient();
// const manager = universal.MakeManager({appName: 'some app', testName: 'some test'})
